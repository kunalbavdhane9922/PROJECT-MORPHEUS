package com.morphus.app.data

/**
 * Repository for registered volunteer phone numbers.
 * Currently uses a hardcoded demo list — can later be sourced from a server.
 */
object VolunteerRepository {

    // Demo volunteers (replace with real numbers or fetch from server)
    val volunteers = listOf(
        "9XXXXXXXXX",
        "8XXXXXXXXX"
    )
}
